spring:
  application:
    name: user
  cloud:
    config:
      # uri: http://localhost:8888
      discovery:
        enabled: true
        # service-id: configserver

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/